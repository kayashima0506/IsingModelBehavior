
/// <summary>
/// ランダムに(N * N)のスピンの向きを決める
/// </summary>
/// <param name=n> N </param>
/// <returns> 二次元配列のスピン </returns>
int setSpin(int n) {

	// ランダムにスピンの向きを決める
	/*
	int spin[N][N];
	srand((unsigned)time(NULL));
	for (int i = 0; i < N; i++) {
		for (int j = 0; j < N; j++) {
			double randomNum = ((double)rand()) / ((double)RAND_MAX + 0) * 1;
			if (randomNum > 0.5) {
				spin[i][j] = -1;
			}
			else {
				spin[i][j] = 1;
			}
		}
	}
	*/
}